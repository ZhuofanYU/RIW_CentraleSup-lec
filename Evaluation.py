from Utils.evaluation import *

if __name__ == "__main__":
    # plot the precision-rappel curve of query 8
    # plotPrecisionRappel(8)

    # plot the synthesis graph of query 3, with threshold 1.344
    # evaluate_threshold(id=3, threshold=1.344)

    # plot the synthesis graph of all 8 queries,only the first 1000 outputs of each query with be considered
    # evaluate_threshold(nb=1000)

    # evaluate develop query 4 with the most pertinent 500 outputs
    # evaluate(id=4, nb=500)

    # plot the synthesis graph of develop query 2
    # evaluate_threshold(id=2)

    # plot the synthesis graph of all 8 queries, time consuming
    # evaluate_threshold()
